import * as React from 'react'
import { WorkplaceWidgetImpl, dataGridProps, addWorkplaceDialogProps, updateWorkplaceDialogProps } from "../../workplaceDashboard/workplaces/WorkplaceWidget"
import { EventTypes } from "../../workplaceDashboard/workplaces/EventTypes"

interface Props {
    
}

const WorkplaceWidget = (props: Props) => {
    return (

    )
}

export default WorkplaceWidget;

const addDialogProps: GenericAddDialogProps<Inputs> = {
    ...addWorkplaceDialogProps,

}

